specVersion: "v1"
serviceId: "cp2"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cp2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
